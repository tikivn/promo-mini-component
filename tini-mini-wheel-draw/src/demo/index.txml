<view class="container">
  <wheel
    width="22em"
    prizeList="{{prizeList}}"
    prizeName="{{prizeName}}"
    rotTimes="{{totalTimes}}"
    onStart="onStart"
    onFinish="onFinish"
    onTimesUp="onTimesUp"
  />
  <view class="times">
    <text>You have {{totalTimes - curTimes}} more chances to draw</text>
  </view>
  
  <view class="result">
    <text>{{result}}</text>
  </view>
  
</view>
